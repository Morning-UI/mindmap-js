/* eslint-disable no-magic-numbers */
import {
    NodeStyle,
}                                               from '../interface';
import {
    LINE_COLOR,
    LINE_WIDTH,
    LINE_RADIUS,
}                                               from './base';

export const MIND_EDGE_STYLE: NodeStyle = {
    radius : LINE_RADIUS,
    borderWidth : LINE_WIDTH,
    borderColor : LINE_COLOR,
};
